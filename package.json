{
  "name": "spstest",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "changeset:version": "changeset version && pnpm install --lockfile-only",
    "changeset:publish": "changeset publish && git push --follow-tags"
  },
  "devDependencies": {
    "@changesets/cli": "2.29.8",
    "turbo": "2.8.5"
  },
  "packageManager": "pnpm@10.29.2"
}
